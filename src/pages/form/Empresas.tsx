import FormTemplate from "./FormTemplate";

const Empresas = () => {
  return (
    <FormTemplate title="GRIFOBOARD MARKETPLACE" subtitle="Formulário de cadastro para empresas">
      <div className="space-y-6">
        <h2 className="text-2xl font-semibold text-foreground">Cadastro de Empresas</h2>

        <p className="text-muted-foreground">Formulário em construção...</p>
      </div>
    </FormTemplate>
  );
};

export default Empresas;
